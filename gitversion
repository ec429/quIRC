#!/bin/sh

git describe --tags | awk -f gv.awk > version.h3

CCVER=$(cc -v 2>&1 | tail -n 1)
echo "#define CC_VERSION	\"$CCVER\" // last line of cc -v" | cat version.h3 - > version.h2
rm version.h3

if ! cmp version.h version.h2; then mv version.h2 version.h; else rm version.h2; fi
